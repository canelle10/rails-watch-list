<div class="navbar">
<h1> Différentes listes</h1>
</div>
  <ul>
  <% @lists.each do |list| %>
    <li><%= link_to list.name,list_path(list), class: "btn btn-secondary"%></li>
    <% end %>
  </ul>

  <%= link_to "Create New List",new_list_path, class: "btn btn-secondary"%>
